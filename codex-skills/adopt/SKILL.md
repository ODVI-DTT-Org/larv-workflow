---
name: adopt
description: Use when the user types /larv:adopt or asks to bring an existing Laravel app under larv management.
---

## larv Headroom Combo

Before context-heavy work, run `bash <larv-plugin-root>/scripts/headroom-combo.sh status "$PWD"` to activate Ponytail + gated Headroom + Caveman. If Headroom falls back, continue normally.

# larv:adopt

This is the Codex-compatible entry point for `/larv:adopt`.

Read `commands/larv-adopt.md`, then invoke the `larv-adopt` skill from `<larv-plugin-root>/skills/larv-adopt/SKILL.md`.

Sub-project A only provides the adoption controller stub. Detailed adoption prompt logic is deferred to sub-project B.
