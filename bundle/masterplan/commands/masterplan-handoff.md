---
description: Phase 8 of MasterPlan — Create agent execution handoff document with exact scope, ordered tasks, verification after every task, blocked-path decision tree, and completion evidence format. Equivalent to invoking the masterplan-handoff skill.
argument-hint: [optional-project-name]
allowed-tools: [Read, Glob, Grep, Bash, Write, Edit, Agent, WebFetch]
---

Use the **masterplan-handoff** skill to run Phase 8: Agent Handoff.

This skill is already active. Follow its instructions exactly.
